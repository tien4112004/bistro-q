<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="BistroQ.Presentation.Views.UserControls.Client.ProductDetailControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BistroQ.Presentation.Views.UserControls.Client"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel>
            <TextBlock Text="{Binding Name}" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="{Binding Price}" FontSize="18"/>
            <TextBlock Text="{Binding Unit}" FontSize="18"/>
            <Image Source="{Binding ImageUrl}" Height="200"/>
            <TextBlock Text="Nutrition Facts:" FontSize="18" FontWeight="Bold"/>
            <TextBlock Text="{Binding NutritionFact.Calories}" FontSize="16"/>
            <TextBlock Text="{Binding NutritionFact.Fat}" FontSize="16"/>
            <TextBlock Text="{Binding NutritionFact.Fiber}" FontSize="16"/>
            <TextBlock Text="{Binding NutritionFact.Protein}" FontSize="16"/>
        </StackPanel>
    </Grid>
</UserControl>
